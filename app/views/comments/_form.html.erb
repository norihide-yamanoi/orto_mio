<%= form_with model: [@blog, @comment] do |f|%>

  <div class="field">
    <%= f.label :content, "コメント編集" %></br>
    <%= f.text_field :content %>
  </div>

  <div class="submit">
    <%= f.submit "送信"%>
  </div>

<% end %>
